import type { Metadata } from "next";
import { Inter, Space_Grotesk } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const spaceGrotesk = Space_Grotesk({
  subsets: ["latin"],
  variable: "--font-space-grotesk",
  display: "swap",
});

export const metadata: Metadata = {
  title: "AdGenius AI — Generate Quality Ads with Creativity Templates",
  description:
    "Generate award-winning ad concepts using the six fundamental creativity templates from Goldenberg, Mazursky & Solomon research. Powered by Google Gemini AI.",
  keywords: [
    "advertising",
    "creative",
    "AI",
    "ad generator",
    "creativity templates",
    "marketing",
  ],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} ${spaceGrotesk.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
